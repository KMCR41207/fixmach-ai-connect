services:
  - type: web
    name: fixmach-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MODEL_PATH
        value: /opt/render/project/src/fixmach_mega_final.keras
